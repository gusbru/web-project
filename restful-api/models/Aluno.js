/*
export default (sequelize, DataTypes) => (
	sequelize.define('alunos', {
		login: {
			type: DataTypes.TEXT,
			allowNull: false,
		},
		senha: {
			type: DataTypes.TEXT,
			allowNull: false,
		},
	}, {
		timestamps: false,
	})
);
*/